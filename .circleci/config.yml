version: 2

jobs:
  build:
    machine: true
    working_directory: ~/repo
    steps:
      - checkout

      - run:
          working_directory: ~/repo/tf-do-meetup/demo3
          name: Config test
          command: |
            echo "Current directory: $(pwd)"
            echo $TF_VAR_do_token 
        
#      - run:
#          working_directory: ~/repo/tf-do-meetup/demo3
#          name: terraform
#          command: |
#            terraform init
#            terraform plan && terraform apply
        
