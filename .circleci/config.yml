version: 2

jobs:
  build:
    machine: true
    working_directory: ~/repo
    steps:
      - checkout

      - run:
          working_directory: $CIRCLE_WORKING_DIRECTORY/demo3
          command: |
            echo "Current directory: $(pwd)"
            echo $TF_VAR_do_token 
            #command: |
            #coterraform init
            #coterraform plan && terraform apply
        
